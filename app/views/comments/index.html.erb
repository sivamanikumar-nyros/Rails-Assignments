<%= link_to "New", new_article_comment_path%>


<table id="customers">
  <tr>
    <th>Article Title</th>
    <th>Name of the commentor</th>
    <th>Comment</th>
  </tr>

  
  <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.article.name %></td>
      <td><%= comment.name %></td>
      <td><%= comment.content %></td>
    </tr>
  <% end %>
</table>
<%= link_to "Back" , articles_path %>